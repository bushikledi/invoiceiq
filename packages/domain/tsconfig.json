{
  // Editor + ESLint project service only — see the note in packages/config.
  "extends": "@invoiceiq/config/tsconfig/library.json",
  "compilerOptions": {
    "noEmit": true,
    "composite": false
  },
  "include": ["src/**/*.ts", "vitest.config.ts", "*.config.mjs"]
}
